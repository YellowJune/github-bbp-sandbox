name: ProofBits native BF16 prefix Pareto sweep
on:
  push:
    paths:
      - 'benchmarks/proofbits_bf16_prefix_sweep.py'
      - '.github/workflows/proofbits-bf16-prefix-sweep.yml'
  workflow_dispatch:
jobs:
  sweep:
    strategy:
      fail-fast: false
      matrix:
        model:
          - Qwen/Qwen2.5-0.5B-Instruct
          - unsloth/gemma-3-270m
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      HF_HUB_DISABLE_TELEMETRY: '1'
      TOKENIZERS_PARALLELISM: 'false'
      PB_MODEL: ${{ matrix.model }}
      PB_N: '2'
      PB_CHUNK: '4096'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: |
          python -m pip install --upgrade pip
          python -m pip install torch 'transformers>=4.55,<5' accelerate sentencepiece
      - run: python benchmarks/proofbits_bf16_prefix_sweep.py
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: proofbits-bf16-prefix-${{ strategy.job-index }}
          path: experiments/artifacts/proofbits_bf16_prefix_sweep_*.json
          if-no-files-found: warn
