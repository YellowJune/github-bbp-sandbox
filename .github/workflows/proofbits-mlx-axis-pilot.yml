name: ProofBits dominant-axis pilot
on:
  push:
    paths:
      - 'benchmarks/proofbits_mlx_axis_pilot.py'
      - '.github/workflows/proofbits-mlx-axis-pilot.yml'
  workflow_dispatch:
jobs:
  bench:
    runs-on: macos-15
    timeout-minutes: 50
    env:
      HF_HUB_DISABLE_TELEMETRY: '1'
      TOKENIZERS_PARALLELISM: 'false'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: |
          python -m pip install --upgrade pip
          python -m pip install --upgrade mlx mlx-lm numpy
      - run: python benchmarks/proofbits_mlx_axis_pilot.py
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: proofbits-mlx-axis-pilot
          path: experiments/artifacts/proofbits_mlx_axis_pilot.json
          if-no-files-found: warn
