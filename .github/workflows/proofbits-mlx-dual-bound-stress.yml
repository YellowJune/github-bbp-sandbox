name: ProofBits dual-bound replicated stress
on:
  push:
    paths:
      - 'benchmarks/proofbits_mlx_dual_bound_stress.py'
      - '.github/workflows/proofbits-mlx-dual-bound-stress.yml'
  workflow_dispatch:
jobs:
  stress:
    strategy:
      fail-fast: false
      matrix:
        replica: [1, 2, 3]
    runs-on: macos-15
    timeout-minutes: 50
    env:
      HF_HUB_DISABLE_TELEMETRY: '1'
      TOKENIZERS_PARALLELISM: 'false'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: |
          python -m pip install --upgrade pip
          python -m pip install --upgrade mlx mlx-lm numpy
      - run: python benchmarks/proofbits_mlx_dual_bound_stress.py | tee experiments/artifacts/proofbits_mlx_dual_bound_stress_replica_${{ matrix.replica }}.log
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: proofbits-dual-bound-stress-${{ matrix.replica }}
          path: |
            experiments/artifacts/proofbits_mlx_dual_bound_stress.json
            experiments/artifacts/proofbits_mlx_dual_bound_stress_replica_${{ matrix.replica }}.log
          if-no-files-found: warn
