name: ProofBits Gemma MLX context sweep
on:
  push:
    paths:
      - 'benchmarks/proofbits_mlx_context_sweep.py'
      - '.github/workflows/proofbits-mlx-context-sweep.yml'
  workflow_dispatch:
jobs:
  bench:
    runs-on: macos-15
    timeout-minutes: 45
    env:
      HF_HUB_DISABLE_TELEMETRY: '1'
      TOKENIZERS_PARALLELISM: 'false'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: |
          python -m pip install --upgrade pip
          python -m pip install --upgrade mlx mlx-lm
      - run: python benchmarks/proofbits_mlx_context_sweep.py
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: proofbits-mlx-context-sweep-gemma
          path: experiments/artifacts/proofbits_mlx_context_sweep_gemma.json
          if-no-files-found: warn
