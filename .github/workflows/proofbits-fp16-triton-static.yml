name: FP16 ProofBits Triton static checks
on:
  push:
    paths:
      - 'kernels/proofbits_triton_fp16_bytes.py'
      - '.github/workflows/proofbits-fp16-triton-static.yml'
  workflow_dispatch:
jobs:
  static:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: |
          python -m pip install --upgrade pip
          python -m pip install torch --index-url https://download.pytorch.org/whl/cpu
          python -m pip install triton
      - name: Syntax and lossless byte-plane self-test
        run: |
          python -m py_compile kernels/proofbits_triton_fp16_bytes.py
          python kernels/proofbits_triton_fp16_bytes.py
